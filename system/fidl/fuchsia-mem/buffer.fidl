
library fuchsia.mem;

struct Buffer {
    // The vmo.
    handle<vmo> vmo;

    // The size of the data in the vmo. This size must be smaller than the
    // physical size of the vmo.
    uint64 size;
};
